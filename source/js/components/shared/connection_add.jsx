var React = require('react');

var ConnectionAdd = React.createClass({
  render: function () {
    return (
      <div className="button add-friend">ADD FRIEND</div>
    );
  }
});

module.exports = ConnectionAdd;
